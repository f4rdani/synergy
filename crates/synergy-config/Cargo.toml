[package]
name = "synergy-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
anyhow = "1.0"
dirs = "5.0"
synergy-proxy = { path = "../synergy-proxy" }

[dev-dependencies]
tempfile = "3.10"
